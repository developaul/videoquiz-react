import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  goBackContainer: {
    margin: '2.5rem 0'
  },
  actionsContainer: {
    display: 'flex',
    justifyContent: 'space-between',
    margin: '2.5rem 0'
  }
}, { name: 'VideoQuizScreen' });